# Rate-Limiting-with-FastAPI
This project demonstrates a simple custom rate-limiting decorator for FastAPI endpoints. The decorator restricts how many requests a user can make within a specified time frame .
